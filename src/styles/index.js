import { Dimensions } from 'react-native';

export const { width, height } = Dimensions.get('window');

export const mainColor = '#16a085';
export const subColor = '#fff';
export const boysColor = '#3498db';
export const girlsColor = '#FF658B';
